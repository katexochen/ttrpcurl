# describe on method with comment fails
exec ttrpcurl --proto comments.proto describe Service.GetValue
[update] cmp stdout describe.comments.Service.GetValue.out
[!update] ! cmp stdout describe.comments.Service.GetValue.out
! stderr .+

# describe on a method with comment succeeds
exec ttrpcurl --proto comments.proto describe GetValueRequest
[update] cmp stdout describe.comments.GetValueRequest.out
[!update] ! cmp stdout describe.comments.GetValueRequest.out
! stderr .+

-- comments.proto --
syntax = "proto3";

// GetValueRequest is a request.
message GetValueRequest {}
message GetValueResponse {}

// Service is a service.
service Service {
  // GetValue gets a value.
  rpc GetValue (GetValueRequest) returns (GetValueResponse) {}
}
-- describe.comments.Service.GetValue.out --
Service.GetValue is a method:
// GetValue gets a value.
rpc GetValue ( .GetValueRequest ) returns ( .GetValueResponse );
-- describe.comments.GetValueRequest.out --
GetValueRequest is a message:
// GetValueRequest is a request.
message GetValueRequest {
}
